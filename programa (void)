#include <iostream>
using namespace std;

void cuadrado();
void rectangulo();
void triangulo();
void circulo();

//declara variables globales
int base, altura;

int main()
{
	int opc; //Variables local
	cout << "CALCULAR AREAS\n";
	cout << "1.Cuadrado\n2. Rectangulo\n3. Triangulo\n4. Circulo\nElige una opcion: \n";
	cin >> opc;

	switch (opc) {

	case 1: cuadrado(); break;
	case 2: rectangulo(); break;
	case 3: triangulo(); break;
	case 4: circulo(); break;
	default: cout << "Opcion incorrecta";
	}
	return 0;
}



void cuadrado() {
	int lado; //Variables local
	cout << "Ingresa el lado del cuadrado: ";
	cin >> lado;
	cout << "El area del cuadrado es: " << lado * lado;

	void rectangulo();
}

void rectangulo() {
	//int base, altura; //Variables local
	cout << "Ingresa la base del rectangulo: ";
	cin >> base;
	cout << "Ingresa la altura del rectangulo: ";
	cin >> altura;
	cout << "El area del rectangulo es: " << base * altura;
}

void triangulo(){
	//int base, altura; //Variables local
	cout << "Ingresa la base del rectangulo: ";
	cin >> base;
	cout << "Ingresa la altura del rectangulo: ";
	cin >> altura;
	cout << "El area del rectangulo es: " << base * altura / 2;
}

void circulo() {
	double radio; //Variables local
	cout << "Ingresa el radio: ";
	cin >> radio;               //M_PI
	cout << "El area del circulo es: " << 3.1416 * radio;
}
